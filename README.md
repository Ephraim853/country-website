# PDF Viewer Website

A beautiful, responsive website to display and view PDF documents with a Canva-like interface.

## Features

- 📱 Responsive design
- 🎨 Modern glass-morphism UI
- 📄 PDF preview and viewing
- ⬇️ Download functionality
- 🖼️ Modal-based PDF viewer
- ✨ Smooth animations and transitions

## Setup Instructions

1. Place your PDF files in the `assets/pdfs/` folder
2. Update the `pdfData` array in `script.js` with your PDF information
3. Open `index.html` in a web browser

## Deployment

### GitHub Pages
1. Push your code to a GitHub repository
2. Go to repository Settings → Pages
3. Select source branch (usually `main` or `gh-pages`)
4. Your site will be available at `https://username.github.io/repository-name`

### Render
1. Create a new Web Service on Render
2. Connect your GitHub repository
3. Use the following build settings:
   - Build Command: `npm install` (if using package.json) or leave empty
   - Publish Directory: `.` (root directory)
4. Deploy

## File Structure
